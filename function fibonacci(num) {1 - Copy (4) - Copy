function fibonacci(num) {
    let a = 0, b = 1, temp;
    while (num >= 0) {
        temp = a;
        a = a + b;
        b = temp;
        num--;
    }
    return b;
}

console.log(fibonacci(6)); // 13
